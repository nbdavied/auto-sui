# 神象云账本 · 抓包指南

目标：逆向神象云账本（Vue SPA）的登录、记账、流水查询接口，用于把 auto-sui 从老随手记迁移到神象云账本。

## 一、入口地址

神象云账本电脑端（随手记企业版界面）：

```
https://login.sui.com/login.jsp?returnUrl=https%3A%2F%2Fyunres.sui.com%2Fpublic-vue%2Fcab-web%2F%23%2Fgeneral&fp=1
```

也可以直接访问 `https://ele.sui.com`。用你平时登录神象云账本的网页地址即可。

## 二、抓包准备（Chrome / Edge DevTools）

1. 打开 Chrome 或 Edge，开一个**无痕窗口**（`Ctrl+Shift+N`），从登录开始抓，能拿到完整认证流程。
2. 按 `F12` 打开开发者工具，切到 **Network（网络）** 面板。
3. 勾选 **Preserve log（保留日志）** 和 **Disable cache（禁用缓存）**。
4. 顶部筛选栏点 **Fetch/XHR**，只看接口请求，排除图片、JS、CSS 干扰。

## 三、操作序列（每步都要在 DevTools 打开状态下做）

按顺序操作，确保每类接口都被抓到：

| 步骤 | 操作 | 目的 |
|---|---|---|
| 1 | 访问网页版并完成登录 | 抓登录 / 认证 / token 接口 |
| 2 | 进入你的**测试账本** | 抓账本列表 / 切换账本接口 |
| 3 | 记一笔「支出」：金额 `1.23`，选一个支出分类 + 一个账户，保存 | 抓支出记账接口 |
| 4 | 记一笔「收入」：金额 `1.23`，选一个收入分类 + 一个账户，保存 | 抓收入记账接口 |
| 5 | 记一笔「转账」：从一个账户转到另一个账户（需两个账户） | 抓转账记账接口 |
| 6 | 打开账户「流水 / 明细」，查询最近一段时间的流水 | 抓流水查询接口（对账去重用） |

## 四、导出与交付

1. 全部操作完成后，在 Network 面板**任意一条请求上右键** → **Save all as HAR with content**。
2. 保存为 `.har` 文件，放到项目目录（如 `C:\Users\nbdav\projects\auto-sui\captures\`），或直接拖进对话发给我。
3. 我会用 `parse_har.py` 解析，提取关键接口。

## 五、注意事项

- HAR 里包含登录 cookie / token，仅限本项目使用，**不要外传**。
- 记账金额用特殊值（`1.23` 或 `0.66`），方便之后在流水里定位验证。
- 测试账本里记的这几笔数据，验证完后可以手动删除。
- 如果 HAR 文件过大，可以只勾选 Fetch/XHR 类型再导出，或对关键请求右键 "Copy as cURL" 单独发我作为补充。
